// CategoryDao 已并入 ../database.dart（DAO 与数据库必须处于同一代码生成库，
// 生成的 xxxCompanion / xxxRow / $xxxTable 才能被 DAO 引用）。
// 保留本文件仅为兼容旧 import 路径（如 lib 其它文件对 daos/category_dao.dart 的引用）。
export '../database.dart';
