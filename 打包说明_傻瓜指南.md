# 记账软件 — 打包成 APK / EXE 傻瓜指南

你好！这份指南专门写给你这样的"纯小白"。你不用懂技术，只要照做就能把软件装到手机上（apk）或电脑上（exe）。

> 现在你的电脑访问不了国外的打包服务器（微软/谷歌/github 都连不上），
> 所以我们用「云端打包」——把代码放到一个自动编译的网站上，网站帮你把 apk/exe 做好。

---

## 你需要准备的

1. 一个**能上外网的设备**（手机就行，或能开代理的电脑）
2. 一个 **GitHub 账号**（免费注册：https://github.com —— 需要能打开这个网站）
3. 我们做好的完整源码包：`ledger_app_full_source.zip`

---

## 方法 A：用 GitHub 自动打包（推荐，能同时出 apk 和 exe）

### 第 1 步：注册 GitHub
- 打开 https://github.com 注册账号（用邮箱，免费）。
- 如果打不开，说明你的网络访问不了，需要开代理/VPN。

### 第 2 步：把源码变成"仓库"
- 登录后，点右上角 `+` → `New repository`（新建仓库）。
- 仓库名随意填，比如 `ledger_app`，选 **Public**（公开）或 **Private**（私有）都行。
- 点 **Create repository** 创建。
- 创建后会进入一个空仓库页面，把页面上的代码复制好（后面要用）。

### 第 3 步：上传源码
> 这里有两种方式，选你觉得简单的：

**方式 1（网页上传，最简单）**：
- 在空仓库页面点 `uploading an existing file`（上传已有文件）。
- 先解压 `ledger_app_full_source.zip`，把里面的**所有文件和文件夹**拖进去（注意要把 android、lib、windows、pubspec.yaml 这些都在，不要只拖一层）。
- 拖完点底部绿色按钮 `Commit changes` 提交。

**方式 2（命令行，需要电脑装 Git）**：在电脑上操作
```
cd 源码所在目录
git init
git add .
git commit -m "first"
git remote add origin 你复制的仓库地址
git push -u origin main
```

### 第 4 步：等待自动打包
- 上传完成后，打开仓库页面上方的 **Actions** 标签。
- 你会看到系统正在自动编译，等约 5~10 分钟。
- 当两个任务（Build Android APK 和 Build Windows EXE）都变成绿色 ✓，就成功了。

### 第 5 步：下载成果
- 点进绿色 ✓ 的任务 → 底部 **Artifacts**（产物）区域。
- 安卓手机用：点下载 `ledger_app.apk`
- Windows 电脑用：点下载 `ledger_app_windows_exe`

### 第 6 步：安装
- **手机**：把 apk 传到手机，点它安装。手机会提示"未知来源"，允许即可。
- **电脑**：解压下载的 zip，双击里面的 `ledger_app.exe` 就能运行。

---

## 方法 B：用"上传即打包"网站（只出 apk，网页操作更少）

像 Code2Native（https://code2native.com）这类网站支持直接上传 zip 自动打包，不用 Git/GitHub 操作，适合完全不想碰命令行的人。

- 注册账号
- 上传 `ledger_app_full_source.zip`
- 选 Android → 点 Build
- 几分钟后下载 apk

---

## 万一打包报错怎么办

最常见的原因是**依赖没下载全或代码没生成**。如果 Actions 里报错：
1. 确认源码包是 `ledger_app_full_source.zip`（这个已经包含生成好的代码）。
2. 确认 `pubspec.yaml`、`android/`、`windows/`、`lib/` 这些都在仓库根目录（不要包在多层文件夹里）。

如果还不行，把报错截图，我可以帮你分析。

---

## 常见问题

**Q：为什么不用我自己的电脑打包？**
A：打包 exe 必须装微软的 Visual Studio（要上微软官网下载），你的电脑访问不了微软服务器，装不上。云端自动带好这些工具。

**Q：apk 能装到苹果手机吗？**
A：不能。苹果手机装软件必须走苹果官方商店（每年 99 美元开发者账号），安卓才可以直接装 apk。

**Q：以后改了代码怎么再打包？**
A：改完后 push 到 GitHub 同一个仓库，Actions 会自动重新编译。

---

祝顺利！有任何一步看不懂，随时问我，我一步步带你。
